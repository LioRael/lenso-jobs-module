{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Lenso Jobs Module configuration",
  "type": "object",
  "required": [
    "schema",
    "database_url_secret",
    "lease_seconds",
    "retry_base_seconds",
    "retry_max_seconds",
    "queues",
    "producer_instances",
    "worker_instances"
  ],
  "properties": {
    "schema": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{0,62}$"
    },
    "database_url_secret": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "lease_seconds": {
      "type": "integer",
      "minimum": 1,
      "maximum": 3600
    },
    "retry_base_seconds": {
      "type": "integer",
      "minimum": 1,
      "maximum": 86400
    },
    "retry_max_seconds": {
      "type": "integer",
      "minimum": 1,
      "maximum": 86400
    },
    "queues": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 128,
        "pattern": "^[A-Za-z0-9._:/-]+$"
      }
    },
    "producer_instances": {
      "$ref": "#/$defs/callers"
    },
    "worker_instances": {
      "$ref": "#/$defs/callers"
    },
    "observer_instances": {
      "$ref": "#/$defs/optionalCallers"
    }
  },
  "$defs": {
    "callers": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 256,
        "pattern": "^[A-Za-z0-9._:/-]+$"
      }
    },
    "optionalCallers": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 256,
        "pattern": "^[A-Za-z0-9._:/-]+$"
      }
    }
  },
  "additionalProperties": false
}
